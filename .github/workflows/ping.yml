name: Keep Render Awake
on:
  schedule:
    # Run every 13 minutes, but only between 6 AM–1:59 AM UTC
    - cron: "*/13 6-23 * * *"
    - cron: "*/13 0-1 * * *"
jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Curl Render app
        run: curl -fs --max-time 10 https://menucrafts.onrender.com/health || true
